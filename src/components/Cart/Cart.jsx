import { useContext } from "react";
import { parentContext } from "../DataProvider";

 

const Cart = () => {
  
    return (
        <div>
            this si cart
        </div>
    );
};

export default Cart;